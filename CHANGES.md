# GOHLEM.AI — CHANGE LOG
# One entry per tagged version. Newest at bottom.
# Tag naming: phonetic alphabet in order (foxtrot → golf → hotel → india → …)
# To restore a version: git checkout <tag>
# To see what changed: git diff <tag> HEAD

| Tag | Date | What changed | Status |
|---|---|---|---|
| v0.1-alpha-first-live-test | 2026-06 | First live Retell test. Baseline before any bug fixes. | WORKING |
| v0.2-india-100pct-benchmark | 2026-06 | Conversation benchmark reached 14/14 static (100%). Simulator added. | WORKING |
| v0.3-bravo-pre-bugfix | 2026-06-25 | Product version Bravo. Known-good before session bug fixes. Benchmark 42/44 (95%). | WORKING |
| v0.4-foxtrot | 2026-06-30 | Baseline before modifier ID retry-storm fix. Includes: Retell integration, token/cost tracking, transcript portal, processing lock (BN5), alias normalization (BN6), price_confirmed bypass (BN7), B04 phantom-item fix, persistent state injection, OrderCart merge logic. Benchmark ~43/46 (rate-limit noise on last run). | WORKING |
| v0.5-golf | 2026-06-30 | Targeted modifier alias fix: MISSING_REQUIRED and INVALID_MODIFIER_ID error responses now return short aliases (M1, M2, M3…) instead of raw 36-char UUIDs. Search results unchanged — real IDs still returned. Prevents UUID transcription errors in AWAITING_MODIFIER cross-turn flows (retry storm). Also adds --only filter to benchmark for chunk-by-chunk runs. Benchmark 43/46 (93%) — same as foxtrot baseline, zero regressions. | WORKING |
| v0.7-india | 2026-07-01 | Filler phrase: retellHandler.js now sends "Sure, one moment." to Retell immediately before the tool loop starts, with content_complete: false. Customer hears acknowledgment within ~100ms instead of sitting in silence through 3+ API calls. shouldSendFiller() skips single-word responses (yes/no/okay) since those turns resolve fast enough that a filler feels redundant; always fires for check-in phrases like "Hello?". Zero change to conversation logic, guards, or benchmark. Benchmark: 42/46 (rate-limit noise on S013; C009 stochastic; S006+S014 pre-existing). | WORKING |
| v0.6-hotel | 2026-06-30 | General scoring fix in gohlem-menu-engine.js. Root cause: global `pie→pizza` WORD_ALIAS corrupted Pizza Place queries — "regular pie" became "regular pizza", and "pizza" in the normalized token matched "Pizza Wrap" instead of items named "X Pie". Fix: (1) Remove `pie`/`pies` from WORD_ALIASES. (2) TOKEN_EQUIVALENTS semantic pairs (`pizza`↔`pie`) — widens scoring matches without changing the query. (3) Negating-modifier check in partial match — prevents "cheeseless" matching "cheese". (4) All-catTokens-matched bonus (+15) when query completely covers category name using equivalence — differentiates "Pizza Pie" from "Pizza Wrap" categories. (5) Unmatched-name-token penalty replaces length-difference penalty — penalizes each name token with no matching query token (catches equal-length wrong matches like "Pizza Wrap" for "cheese pizza"). (6) Description phrase match bonus (+15) when full query appears as substring in description — differentiates "Regular Pie" (desc: "Classic cheese pizza…") from other pies for "cheese pizza" query. (7) phraseAlias `cheese pie → cheese pizza` added — restores exact substring match for Cheese Pizza on Tony's (fixes clarification that would have fired at gap=13). (8) Update Regular Pie description to include "cheese". Tiebreaker in findItems: fewer unmatched name tokens, then shorter name. Benchmark: 44/46 (95.6%) vs 43/46 baseline — zero regressions, one net improvement. | WORKING |
